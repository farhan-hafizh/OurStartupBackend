name: cd

on:
  push:
    branches: 
      - master

jobs:
  cd: 
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Deploy using SSH
        uses: appleboy/ssh-action@master